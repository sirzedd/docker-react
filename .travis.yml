sudo: required
services:
    - docker

before_install:
    - docker build -t sirgreentoad/docker-react -f Dockerfile.dev .

script:
    - docker run -e CI=true sirgreentoad/docker-react npm run test -- --coverage